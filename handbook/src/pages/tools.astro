---
import Doc from "../layouts/Doc.astro";
import CheckMeter from "../components/CheckMeter.astro";
import CheckPipeline from "../components/CheckPipeline.astro";
import ItemActions from "../components/ItemActions.astro";
import SchemaSheet from "../components/SchemaSheet.astro";
---

<Doc title="数值与工具调用" wide>
  <h1>数值与工具调用</h1>
  <p class="lead">
    生命值、理智、技能、位置、背包、标记和已知线索，都只能经由工具调用、由程序写入。调查员卡本身是只读的。
  </p>
  <CheckMeter />
  <h2>检定必须走工具调用</h2>
  <p>
    模型不能自己报点数。工具调用只允许带三项：谁、用什么技能、对着哪一件道具。程序会先查调查员卡、再查目标表，然后掷骰、比对，最后把结果交回；对话里按这份结果画出检定尺。
  </p>
  <CheckPipeline />
  <h2>检定提议</h2>
  <SchemaSheet id="check-in" />
  <h2>检定结果</h2>
  <SchemaSheet id="check-out" />
  <div class="split">
    <div class="callout ok">
      <strong>这样检定</strong>
      <p>调用「检定」，传入谁＝林晚、技能＝开锁、目标＝书桌锁；等程序返回失败之后，再写「锁没开」。</p>
    </div>
    <div class="callout warn">
      <strong>这样不算</strong>
      <p>在叙述里直接写「你掷出 3，锁开了」，或者把点数和成败塞进工具参数里。</p>
    </div>
  </div>
  <h2>对道具的动作怎么画</h2>
  <p>检定只是其中一种动作。观察、取走、阅读，以及被拒绝的动作，同样要让人看见目标和结果。点下面的格子可以逐个对照。</p>
  <ItemActions />
  <h2>程序认哪些调用</h2>
  <table>
    <thead>
      <tr>
        <th>工具调用</th>
        <th>程序做什么</th>
        <th>例子</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>移动</td>
        <td>检查出口与条件，改当前位置</td>
        <td>门厅 → 书房</td>
      </tr>
      <tr>
        <td>检定</td>
        <td>掷骰，比较技能与难度，记成败</td>
        <td>开锁 45，掷 42，难度 60，失败</td>
      </tr>
      <tr>
        <td>转移道具</td>
        <td>检查是否拿得到，改道具所在</td>
        <td>账本：书房 → 背包</td>
      </tr>
      <tr>
        <td>改资源</td>
        <td>按规则加减生命值或理智</td>
        <td>读账本，理智 −5，65 → 60</td>
      </tr>
      <tr>
        <td>记录线索</td>
        <td>把一条线索标为该调查员已知</td>
        <td>得知港口交易时间</td>
      </tr>
      <tr>
        <td>应用房规</td>
        <td>改某条规则数字，并记改前改后</td>
        <td>开锁难度 60 → 40</td>
      </tr>
    </tbody>
  </table>
  <p>
    如果叙述里写了「你掉了 5 点理智」，却没有「改资源」这笔提交，那么数据卡上的理智必须仍然是 65。数据卡可以高亮刚刚变化的格子，但不提供输入框；玩家想改数字，只能请守秘人走「应用房规」。
  </p>
</Doc>
