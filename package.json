{
  "name": "ai-trpg-engine",
  "packageManager": "bun@1.3.13",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": ["electron"],
  "scripts": {
    "desktop": "bun run --cwd electron dev",
    "desktop:check": "bun run --cwd electron persist:check",
    "test": "bun run --cwd electron test",
    "build": "bun run --cwd electron build",
    "package:win": "bun run --cwd electron package:win"
  }
}
