# 实现计划

把 [05-里程碑.md](05-里程碑.md) 落到本仓库上。V1.0 的模块接口、DDL、IPC 以 [`docs/05-implementation-design/`](../docs/05-implementation-design/README.md) 为准（Draft）。共用同一条提交边界的工单只能串行，其余的可以并行。

## 0. 现状与差距

两套代码，不要混着读：

| 部分 | `demo/`（试玩切片） | `src/` + `src-tauri/`（桌面外壳） | V1 Draft 要变成什么样 |
| --- | --- | --- | --- |
| 外壳 | 浏览器，无 Tauri | Tauri 2，Rust 只管钥匙串和插件 | Electron 主进程 + `DesktopApi`；渲染进程不许碰库 |
| 权威 | 事件只追加、检查点、分支、重放 | CRUD：战役／场次／消息／人物 | `turns` + `events` + `state_entities` + `rule_decisions` 同事务提交 |
| 模型 | Ollama + Zod 契约，写不了事实 | `src/lib/tools.ts` **会写库** | `gm.interpret_action`／`gm.narrate_result`，Candidate ≠ 事实 |
| 资料包 | 八个 JSON 目录，`pack:lint` | 静态 `src/data/items.ts` | ZIP `.scenario-pack`，带 `manifest.json` 与 SHA-256 |
| 跑团桌 | 叙述／人物卡／房间／时间线 | 会话聊天页 | `/campaigns/:id/play`，无乐观权威写入 |

W1–W4 的游戏语义（提交、重放、主持人写不了事实、寄宿公寓资料包、跑团桌）**已经在 `demo/` 做完**。`src/` 还停在「会写库的聊天工具」。下一步不是在 `src/engine/` 从零再写一遍，而是：

1. 把外壳决策写成 Accepted（跟 V1 走 Electron，或改 V1 承认 Tauri）。
2. 按 V1 Persistence／IPC 把 Demo 内核搬进主进程。
3. 停掉桌面工具写库这条路。

在提交循环在桌面侧也立起来之前，不要加导演、记忆模型和人设卡导入。

---

> W1–W4 的验收已经在 `demo/` 达成（状态哈希 `b6506aeb`）。下面原文保留，当作那一轮的规格，不要再按「新建 `src/engine/`」开工。桌面侧从 **W0** 接。

## W1 — 权威内核（M0）

**结果：** 程序能提交一个回合，并且能重放。

1. 新建 `src/engine/`，定义 `entity_id`、`branch_id`、`turn_id`、`state_version` 和事件信封。
2. 做 SQLite 迁移：
   - `branch`、`turn`、`event`、`rule_decision`
   - 房间、道具实例、调查员、NPC、线索、剧情标记、开关各自建表（三个房间的规模用表就够，不必整包塞文档）
3. 实现 `Runtime.commit`：版本检查、全有或全无、按 `turn_id` 幂等。
4. 掷骰时记录种子，重试不重新掷。
5. 把 `check.ts` 收到 `rules.resolve(intent, state, seed)` 后面。
6. 写无界面测试：脚本化地跑「开书桌锁」的失败和成功，哈希必须稳定。

**停在哪：** 聊天界面仍然可以用旧工具，但测试已经证明新内核成立。

---

## W2 — 回合路由与主持人契约（M0）

**结果：** 模型再也写不了事实。

1. 定义 Zod 联合类型：`clarification`｜`context_request`｜`action_intent`｜`narration`。
2. 替换掉那些会写库的工具（`update_character`、战斗相关的变更、被当成权威用的 `write_note`）：
   - 读取一律经过上下文调度（道具、房间、人物卡的视图）
   - 1 级和 2 级由运行时自己落实
   - 主持人只在 3 级输出 `action_intent`
3. 定义回合的各个阶段：接收 → 路由 →（澄清）→ 裁定 → 提交 → 叙述。
4. 界面加一条处理提示：「正在检定书桌锁」，但不报成败。
5. 如果叙述把仍然锁着的书桌写成打开了，就拒绝这段叙述，按同一版本重试。

**实验记录：** 在只有三个房间的地图上，2 级快匹配到底值不值得（「进书房」「取走账本」这类多半是值得的）。

---

## W3 — 把寄宿公寓做成资料包（M1）

**结果：** 手册里的东西能真正玩起来，而不只是一张图解。

1. 把手册里的房间、道具、动作、剧情标记和条件写成 JSON，放进 `src/data/packs/boarding-house/`。
2. 实现剧本运行时：每次提交之后判定条件，需要立即触发的就放进同一个事务。
3. 实现可见性：玩家视图与守秘人视图（道具的 `keeper` 字段已经有雏形）。
4. 开团流程：预组的林晚，出生点为 `loc.hall`，免掉完整的车卡回。
5. 这份资料包里不再出现 5e 专用的措辞。

**停在哪：** 测试能走通「门厅 → 书房 → 开锁失败 → 再开锁成功 → 取走账本 → 阅读 → 理智降到 60 → 剧情标记完成」这一条路。

---

## W4 — 跑团桌（M1）

**结果：** 试玩的人不会觉得这只是套了层皮的聊天窗口。

1. 聚会页面：叙述列居中，人物卡和房间小地图常驻。
2. 建议行动的芯片来自上一条 `narration.suggested_actions`。
3. 已有的检定卡改为绑定 `rule_decision`，而不是绑定模型写的散文。
4. 可以展开的部分：背包、线索、剧情标记、事件记录。
5. 存档和读档都走检查点加事件，明确拒绝「把聊天记录导出来当存档」。
6. 把导演和提示词藏起来，调试路由等以后再说。

到这里做一轮试玩。如果大家记住的还是「那个对话框」，说明桌子没做完。

---

## W0 — 外壳与权威边界（挡在 W5 前面）

**结果：** 桌面不再让渲染进程写事实。

1. 评审 [`docs/05-implementation-design/`](../docs/05-implementation-design/README.md)，把 Electron／Tauri 写成 Accepted。
2. 按 [02-ipc-contracts](../docs/05-implementation-design/02-ipc-contracts.md) 露出 `campaign`／`turn`／`timeline`／`operation`，渲染进程丢掉直接 SQL。
3. 把 `demo/src/store` 的表扩到 V1 Campaign DDL 的最小子集：至少补 `turns`、`rule_decisions`、`state_entities`。
4. 禁用 `src/lib/tools.ts` 里一切写库工具。
5. 资料包从八个 JSON 目录迁到 [14-content-system](../docs/05-implementation-design/14-content-system.md) 的 `.scenario-pack`（可先做单向打包）。

**停在哪：** Demo 金样哈希仍然是 `b6506aeb`；桌面提交一条行动只走 IPC，不再 `UPDATE characters`。

---

## W5 — 可分发的表面（M2）

1. 首次启动：选供应商、测连通性、创建战役。
2. 设置页面对齐 V1 的 `provider_instances`／`model_profiles`／`task_routes`。
3. 人设卡导入（常见的 Markdown 和 JSON），能力标记为「人设卡」。
4. 资料包校验走 content CLI：`validate`／`pack`／`inspect`。
5. 按 [18-release-compatibility](../docs/05-implementation-design/18-release-compatibility.md) 出 Windows x64 包；macOS 开发流不当验收。
6. 模型失败时：给出提示并允许重试叙述，已经成功的提交不回滚。

---

## W6 — 记忆与导演（M2 末尾接触，正经做在 M3）

必须排在 W0 之后。规格对齐 [11-ai-orchestrator](../docs/05-implementation-design/11-ai-orchestrator.md) 与 [13-context-memory](../docs/05-implementation-design/13-context-memory.md)。

1. 记忆：批量整理，并且**引用事件编号**，不删除原始事件（V1：`memory_entries` + FTS5）。
2. 导演：做一张触发表（线索丢失、NPC 警戒、剧情停滞），输出 `ScenarioSuggestion`，不直接改节点状态。
3. 临时上下文：当前房间加背包加未完成的剧情标记，读档时重建。
4. 分支与回滚的界面 Demo 已经有了；桌面要走 `timeline:`／`campaign:` IPC，接到 V1 `checkpoints`。

---

## 建议的推进顺序

```
W1–W4 已在 demo/ 做完
        │
        ▼
W0 外壳决策 + IPC + V1 DDL 子集 + 停掉工具写库
        │
        ▼
W5 分发表面 = V0.1（Windows x64）
        │
        ▼
W6 导演／记忆 = V0.x（11、13 两份设计）
```

一个人做：先把 W0 写成能验收的差距，再碰 W5。不要平行再开一套 `src/engine/`。

---

## 头两周（历史，W1–W4 当时的排期）

**第一周**

- 引擎的类型定义，以及 `event` 和 `turn` 两张表
- 把开锁检定迁进内核，配一个固定种子的测试
- 禁止主持人传入 `roll` 和 `ok`（工具模式里已经限制，运行时再强制一次）

**第二周**

- 房间占用，以及道具实例的移动与拒绝
- 条件联动：书桌打开之后，账本才可以取走
- 让一条聚会路径走新的提交流程，旧界面先凑合着用

之后打磨 W4，再做试玩。

---

## 风险

| 风险 | 对策 |
| --- | --- |
| 模型不守契约 | 用 Zod 直接丢弃，非法对象不产生任何副作用 |
| 快路径误判 | 保守处理，只匹配房间内唯一的那个动词 |
| 把聊天记录当存档 | 读档只读事件 |
| 范围膨胀（战斗、SRD、多人） | 一律搁置，试玩就只有三个房间 |
| 5e 的残留撞上克苏鲁演示 | 由资料包来决定提示词和人物卡 |
| 把整张道具总表塞进提示词 | 交给上下文调度：只给当前房间、背包和可做的动作 |

---

## 每个改动的完成定义

- 新增的提交路径都有测试
- 不新增任何会写库的主持人工具
- 机械性行动无论被接受还是被拒绝，都要写事件
- 领域不变量变了，就同步改手册或者本目录里的需求
